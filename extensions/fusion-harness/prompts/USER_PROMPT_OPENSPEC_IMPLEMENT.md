Implement OpenSpec change {{CHANGE}}, phase {{PHASE}} — {{PHASE_TITLE}}.

Phase order is a hard barrier. Complete only these tasks:
{{TASKS}}

Run the listed verification commands. Do not mark tasks complete; the host does that only after verification passes.

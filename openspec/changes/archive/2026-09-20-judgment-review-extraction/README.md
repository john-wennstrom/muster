# judgment-review-extraction

When a planning reviewer returns prose instead of the required object, recover the review by classifying the reviewer's own lines with typed judgment instead of running a corrective retry.

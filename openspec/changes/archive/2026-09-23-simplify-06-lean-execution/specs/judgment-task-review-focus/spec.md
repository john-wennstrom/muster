## REMOVED Requirements

### Requirement: Judgment never changes a review gate
**Reason**: A review may now be skipped when every guard holds, so judgment can change whether a reviewer runs.
**Migration**: See the `review-skipping` capability, which defines the guards, the labelled record and the final-validation rule that replace this requirement.

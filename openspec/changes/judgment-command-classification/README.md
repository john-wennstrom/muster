# judgment-command-classification

Widen the manual-approval check for brokered host commands with a typed judgment that can only add a category for commands the unchanged rule floor would otherwise allow.

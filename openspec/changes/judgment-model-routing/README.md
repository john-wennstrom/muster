# judgment-model-routing

Route mechanical tasks to an explicitly configured economy builder lane using a per-task typed judgment, flag-gated and heavily guarded, integrated where production actually chooses the builder model.

## MODIFIED Requirements

### Requirement: The debug flag is accepted

The command line SHALL accept `--debug` to enable debug output.

#### Scenario: Debug flag enables debug output

- **WHEN** the command runs with `--debug`
- **THEN** debug output is printed